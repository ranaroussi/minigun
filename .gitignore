# Local secrets — must never be committed
credentials.md
.env
.env.*
!.env.example

# Drafts / personal writing that shouldn't ship in the public repo
x.md
DRAFT*.md
draft*.md

# Internal design / scope docs that aren't public-facing
docs/proposals/

# Go
bin/
# Built binaries (anchored so cli/cmd/minigun/ package dir is NOT ignored)
/cli/minigun
/src/minigun
*.test
*.out
coverage.out

# Local SQLite databases used in dev / smoke tests
*.db
*.sqlite
*.sqlite3
*.db-journal

# Cloudflare Worker / Node
node_modules/
.wrangler/
dist/
build/
.miniflare/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
